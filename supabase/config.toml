# Supabase CLI project config.
# project_id is filled in when you run `supabase link --project-ref <ref>`.
# This file is committed; secrets live in .env.local.

project_id = "sghfmudgnddybsayfqbd"

[db]
major_version = 17
