# Pending Living Doc Updates buffer
# Format per line: TIMESTAMP | Fix LABEL: Description | Section
# Example:        2026-05-26T18:30Z | Fix MAIL-007: Lob fallback when address validation returns soft-fail | Mail Module
#
# Entries here render in the "Pending Buffer" section of the Living Doc until they ship and get moved into CHANGELOG.md under a real version header.
# Lines starting with '#' and blank lines are ignored by the parser.

2026-06-25T13:50Z | Fix LOB-WEBHOOK-SIGNATURE-TIMESTAMP: Lob webhook signature verification was hashing only the raw body, but Lob's format is HMAC-SHA256 over `${Lob-Signature-Timestamp}.${body}`. Handler now reads the lob-signature-timestamp header, builds the concatenated signature input, and HMACs against that. Latent bug since at least May 2026 caused every real Lob webhook delivery to return 401. | Mail / Webhook
2026-06-25T12:00Z | Fix LOB-MICRODEPOSIT-DUAL-MODE: Reverses LOB-REJECT-DESCRIPTOR-BANK fintech guard. Now stores Lob's microdeposit_type on each row (migration 0161). Verify Manually modal branches to either two-amount inputs (traditional banks) or a 6-character SM-prefixed code input (fintech banks like Mercury, Brex, Novo). New verifyMailBankAccountWithCode server action. lobVerifyBankAccount helper now takes a LobVerifyInput union. Customer never sees a bank-type error; the right form just appears. | Mail / Bank Accounts
2026-06-22T12:30Z | Fix LOB-SCRAPER-REWORK: Published-rate sync moved off the volatile llms-full.txt corpus onto the stable GitBook pricing-details article. New parser walks the HTML tables embedded in the markdown. Failure path now emails ops (OPS_ALERT_EMAIL, default bree@mossequitypartners.com) via Resend instead of writing into the customer-facing notifications bell. Cron also detects future-effective rate sections Lob has announced on the same page (Lob currently has July 12, 2026 changes published) and emails ops with the diff. Per-org snapshot still updates for the Settings > Lob Pricing UI. | Mail / Pricing
2026-06-22T12:45Z | Fix PLAID-CLEANUP: Removed all dead Plaid integration code. Plaid Link UI was already gone from the Add Bank drawer (Lob doesn't accept Plaid processor tokens; manual entry is simpler than an OAuth dance for the same result). This PR removes the orphaned exchangePlaidPublicTokenForBankAccount + createPlaidLinkToken server actions, the every-4-hour plaid-microdeposit-poll cron (was a no-op; zero rows have plaid_access_token), the @/lib/plaid/client module, and the plaid + react-plaid-link package deps. DB columns retained since dropping needs a migration with negligible benefit. | Mail / Bank Accounts
2026-06-19T23:30Z | Fix COOKIE-CONSENT: Banner only shows once per browser. Server snapshot returns the consented sentinel so SSR markup never includes the banner; client snapshot reads localStorage after hydration. Dismiss writes localStorage and notifies a module-level listener set so the snapshot re-reads in the same tab. Eliminates the flash on every full server render (login redirect, hard nav, reload). | Legal / Cookie Consent
2026-06-19T23:00Z | Fix KANBAN-SCROLL: Sticky stage header owns its top border (border-t moved off the column wrapper onto the header so it stays visible while scrolling; box-shadow attempt was being clipped by the parent's overflow). Scrollbar palette moved to true achromatic neutral (#d4d4d4 / #a3a3a3) since slate had a blue cast. Anchored to Linear / Attio / Pipedrive. | Leads / Kanban
2026-06-19T21:52Z | Fix OAUTH-001: Gmail Connect callback no longer hangs the user on a blank /api/oauth/google/callback URL during the 5-30s initial backfill. Sync now runs via Next 16 `after()`; the redirect to /settings?email_connect=success fires immediately. Fluid Compute keeps the function alive past the response; if evicted, the per-minute sync cron catches the account up. | Email / OAuth
2026-06-19T20:00Z | Fix AUTOMATIONS-001: New Automations section (owner-only). Web Form module configures a public inquiry form at /f/<formId> with assignee (single or round robin), pipeline stage routing, and editable success message. Submissions create lead + owner + contacts + activity + follow-up task and email the assignee via Resend. CORS-enabled JSON API at /api/form/<formId> for cross-origin posts (mossequitypartners.com is allowlisted so the rebuilt MEP site can post directly). Spam = honeypot + per-IP rate limit. Migration 0152 adds web_forms, web_form_submissions, web_form_rate_limits with org-scoped RLS and seeds one row per existing org. | Automations
2026-05-27T16:30Z | Fix WWWW4: Playbooks board (cross-lead Kanban per research template) | Playbooks
2026-05-27T17:00Z | Fix WWWW5: Drop orphan lead_latest_activity view + renumber stuck 0120_revert_clearout to 0135 | Migrations / Infra
2026-05-27T18:00Z | Fix XXXX4: Lead-detail stage strip scrolls and auto-centers on current stage | Leads / Detail
2026-05-27T19:00Z | Fix YYYY4 part 1: Custom stages schema + types + server query + CRUD actions (no UI yet) | Custom Stages
2026-05-27T20:00Z | Fix YYYY4 part 3: Kanban + advanceStage refactor to use stage_id; dynamic column rendering | Custom Stages
2026-05-28T14:00Z | Fix ZZZZ4: Bank account delete now propagates to Lob (DELETE /v1/bank_accounts/{id}); prevents orphan accounts in Lob after add/delete cycles | Mail Module
2026-05-28T15:00Z | Fix YYYY4 part 9: Dashboard Pipeline Funnel narrowing-trapezoid (Option C); Won/Lost split out as outcome cards | Dashboard / Custom Stages
2026-05-28T16:00Z | Fix YYYY4 part 12: Pipeline Funnel section removed from dashboard, will revisit. Funnel-options.html mockup retained | Dashboard / Custom Stages
2026-05-28T20:00Z | Fix AAAA5: /inbox now scoped to caller's own channel_accounts. Rik's lead-linked threads no longer pile into Bree's Inbox. Lead Conversations tab unchanged, still shows the full org thread per the hybrid RLS design. Also resolves silent click-no-op for threads owned by another user (ThreadReader fell through because accountForReader couldn't match). | Inbox
2026-06-15T02:10Z | Fix TEAM-ICON-001: Restore visible three-dot overflow + breadcrumb chevrons + Invite Member glyph on Settings > Members. CSS-class icons (icon-more-horizontal, icon-chevron-right, icon-user-plus) were never wired to glyphs in the settings clone, so the per-row member-actions trigger and breadcrumb arrows were invisible and rows looked unactionable. Swap to Tabler React components (IconDots / IconChevronRight / IconUserPlus) consistent with the rest of the codebase. | Settings / Team
2026-06-15T01:30Z | Fix RESEND-001: Switch outbound transactional email from bree@mossequitypartners.com to noreply@nextsurplus.com (new Resend sending domain) across team invites, phone-validate threshold alerts, and Lob cap notifications. RESEND_FROM env updated in Production and Preview; code falls back to the same Title-cased default if env is missing. | Mail Module / Email
2026-06-15T03:45Z | Fix TEAM-PENDING-001: Pending invite rows on Settings > Team now show Resend Invite + Cancel Invite (Linear/Notion/Slack/Attio/Vercel standard) instead of the active-member Make Admin/Member + Remove From Org. Adds resendInvite + cancelInvite server actions with admin guard + cross-org guard + audit log entries. Resend regenerates the invite token (deletes-then-recreates the auth user) so the link in the new email is always fresh. | Settings / Team
2026-06-15T01:50Z | Fix SUPABASE-REF-ROTATION: Original Supabase projects were accidentally deleted. Rotate every reference from old staging sghfmudgnddybsayfqbd to new qfanroxcoepunmrmjabo and from old prod qvyhdexoicoppgrvvtov to new rsdmyydyhqgkkvwlklif. Updates CLAUDE.md guard rule, README setup steps, ARCHITECTURE topology + ADR-003, supabase/config.toml, supabase/.temp/{project-ref,linked-project.json,pooler-url}, and all four migration / seed / test scripts. Vercel env vars already point at the new projects; this PR brings the repo back in sync. | Infra / Supabase
2026-06-16T20:00Z | Fix 6: app_pricing_config.subscription_monthly_cents 7900 -> 4900 for the Founders Rate window. Single-row UPDATE in migration 0149. | Billing / Pricing
2026-06-16T20:00Z | Fix 12: plan_tier_enum on orgs (founder, beta_founder). Founder Rate cutoff date on app_pricing_config (seeded current_date + 60 days, placeholder). BEFORE INSERT trigger assigns beta_founder while the window is open. Moss Equity Partners pinned to founder. Migration 0150. | Billing / Pricing
2026-06-16T20:00Z | Fix 7: /privacy and /terms pull subscription pricing from app_pricing_config at render time. Physical mail + Plaid + 30 day read only window removed from both. CI workflow legal-no-hardcoded-prices.yml fails the build if any $NN literal returns to the legal pages. | Legal / Pricing
2026-06-16T20:00Z | Fix 13: Stripe billing. /api/stripe/create-checkout-session + /api/webhooks/stripe (signature verified) + /api/stripe/create-portal-session. Migration 0151 adds stripe_customer_id, stripe_subscription_id, subscription_status, last_invoice_paid_at, founder_notice_sent_at to orgs. Daily cron /api/cron/founder-lock-expiration (12 UTC) sends a 10 month renewal heads up email. Stripe Dashboard work (two recurring prices + three Vercel env vars) handed back to Bree. | Billing / Stripe
2026-06-16T20:00Z | Fix 10b: Landing v1 FAQ rewritten for current product reality. Founder Rate pricing block on the landing page updated to $49 / Limited Window. Physical mail + Plaid + Outlook bullets dropped from the pricing card. | Landing / Pricing
2026-06-16T22:30Z | Fix 14: Standard tier removed. priceIdFor no longer accepts "standard", STRIPE_STANDARD_MONTHLY_PRICE_ID env var dropped, founder-lock cron becomes a 10 month renewal heads-up only (no auto-promote). Landing mockups v3/v4/v5 realigned to $49. setup-stripe-products.mjs provisions only beta_founder_monthly + beta_founder_annual. Live mode standard_monthly price archived in Stripe. | Billing / Pricing
2026-06-16T22:15Z | Fix 10d: Strip Outlook + "coming next" language from landing v1. Feature card 02 title becomes Gmail-only (Outlook/IMAP not QA-validated); subtitle below the pricing card reading "Microsoft Outlook and IMAP inbox sync are coming next." removed entirely. | Landing / Copy
2026-06-16T21:30Z | Fix 10c: Landing v1 FAQ rewrite v6. Trimmed from nine to six questions. Email / Gmail question dropped entirely (email is plumbing, not a differentiator). Pattern-matched against Pipedrive / HubSpot / Monday / Close / Copper. Removed every Q whose answer already lives in the pricing card or feature grid; added Getting Started, CSV Import, Per Seat Fees, and Support questions. Title Case headers, no "coming soon" language. | Landing / FAQ
2026-06-16T23:00Z | chore(brand): Next Surplus logo + favicon pack deployed to public/. Favicon set (favicon.ico, favicon-16/32/48.png), apple-touch-icon.png, android-chrome-192x192.png, android-chrome-512x512.png wired through Metadata.icons in src/app/layout.tsx. New public/manifest.json registers theme #04261c + background #faf7f2 for PWA installs. Ten brand SVGs staged at public/brand/ for future component swaps. Open Graph block points at nextsurplus.com/og-image.png and public/og-image.png (1200x630) committed so social shares render. | Branding / Assets
2026-06-17T00:00Z | Fix 15: Signup + Login + Verify + Onboarding. /signup-mockups + /login-mockups galleries with 10 anchored variants (Linear / Attio / Folk / Pipedrive / Vercel / Stripe / Supabase) under new (mockups) route group. Real /signup wired to signUp server action that creates Supabase user + org + owner profile then Stripe Checkout with client_reference_id + metadata.org_id. /signup/verify polls new /api/billing/subscription-status endpoint every 2s, Loading / Success / Stalled-at-30s states, auto-redirects to /onboarding/firm on active or trialing. 4-step /onboarding/[step] wizard (Firm / Import / Inbox / Team), skip allowed on 2-4, Plus Jakarta Sans Medium via Google Fonts. | Signup / Onboarding

2026-06-19T17:00Z | Fix BANK-VERIFY-AUDIT: Lob micro-deposit verification flow hardened. Verify Manually modal added on every unverified bank card via new verifyMailBankAccountManually server action. Cron plaid-microdeposit-poll now filters Plaid transactions by merchant name (only credits matching LOB qualify) so random sub-$1 credits cannot burn Lob 3-try limit. New verify_attempts + last_verify_error + last_verify_attempt_at columns on mail_bank_accounts (migration 0155); cron caps auto-attempts at 2 so the third try stays manual. Vercel cron cadence bumped from daily to every 4 hours. Bank card shows last-checked time + attempts + last error. Drawer copy rewritten. | Mail Module

2026-06-19T23:30Z | Fix COOKIE-CONSENT: Cookie banner only shows once per browser. Root cause: useSyncExternalStore returned null from the server snapshot, so SSR markup always included the banner and hydration hid it, causing a visible flash on every full page render (login redirect, hard navigation, page reload). Rewrote CookieConsent.tsx to start show=false and flip to true only inside useEffect when no consent is found in localStorage. After one OK click the value persists and the banner never reappears. | Legal / Cookie Consent

2026-06-19T18:00Z | Fix BANK-MANUAL-ENTRY: Add Bank Account drawer now has a manual routing + account number entry mode that bypasses Plaid Link. Used when Plaid env vars are misconfigured (operator hit client_id must be a properly formatted, non-empty string on prod with no fallback) and for operators who prefer typing. New addMailBankAccountManually server action validates routing 9 digits + account 4-17 digits + holder, sends to Lob bank_accounts, inserts the row with verified_via=micro_deposits. The existing Verify Manually modal handles the final verification step. | Mail Module
